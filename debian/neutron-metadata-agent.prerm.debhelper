# Automatically added by dh_installinit
if [ -x "/etc/init.d/neutron-metadata-agent" ] || [ -e "/etc/init/neutron-metadata-agent.conf" ]; then
	invoke-rc.d neutron-metadata-agent stop || true
fi
# End automatically added section
