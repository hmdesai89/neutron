# Automatically added by dh_installinit
if [ -x "/etc/init.d/neutron-metering-agent" ] || [ -e "/etc/init/neutron-metering-agent.conf" ]; then
	invoke-rc.d neutron-metering-agent stop || true
fi
# End automatically added section
